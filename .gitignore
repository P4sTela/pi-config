# Secrets — never commit
auth.json
models.json

# Session history / run history (may contain sensitive data, changes constantly)
run-history.jsonl
sessions/

# Machine-local state
trust.json

# Packages installed via `pi install` (re-installable, not source)
git/
npm/

# OS / editor cruft
.DS_Store
*.swp
node_modules/
